#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main(){
    cout<<"1";
   /* while(t--){
        ll x,y;
        int n;
        cin>>x>>y>>n;
        int A[n],H[n];
        for(int i=0;i<n;i++)
        cin>>A[i];
        for(int i=0;i<n;i++)
        cin>>H[i];
 
        bool die=false;
        for(int i=0;i<n;i++)
        {
            if(y<A[i] && x<H[i]){die=true;break;}   
            ll ans;
            if(x%H[i])
            ans = (x/H[i])+1;
            else 
            ans = x/H[i];
            y=y-(ans*A[i]);
        }
        if(die)cout<<"NO";
        else cout<<"YES";
        cout<<"\n";*/
       
    //}
    return 0;
}